<Page
    x:Class="PokeGo.PokeDex"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PokeGo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Controles de filtrado -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20">
            <TextBox x:ConnectionId='8' x:Name="txtFiltroNombre" PlaceholderText="Nombre" Margin="5" Width="125" />
            <ComboBox x:ConnectionId='9' x:Name="cmbFiltroCategoria" PlaceholderText="Categoria" Margin="5" Width="118" />
            <Button x:ConnectionId='10' Content="Filtrar" Margin="5" Background="#330404E8" Height="30" Width="93" Foreground="#FFF71313"                       />
        </StackPanel>

        <!-- Lista de Pokemon -->
        <ListView x:ConnectionId='2' Grid.Row="1" x:Name="lstPokemon" Margin="20" FocusVisualPrimaryBrush="#FF1A85A4"                                               >
            <ListView.ItemTemplate>
                <DataTemplate                           >
                    <StackPanel x:ConnectionId='4' Margin="5">
                        <TextBlock x:ConnectionId='5'                        Style="{ThemeResource BaseTextBlockStyle}" FontSize="18" />
                        <TextBlock x:ConnectionId='6'                        Style="{ThemeResource BodyTextBlockStyle}" />
                        <Image x:ConnectionId='7'                          Stretch="Fill" Width="150" Height="200"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>

